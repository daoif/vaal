# 项目级约束

> 此文件定义适用于整个项目的约束。
> 所有任务都会自动继承这些约束。

---

## 硬约束

- 所有公共 API 必须有 JSDoc 注释
- 禁止使用 `var`，必须使用 `const` 或 `let`
- 所有异步操作必须有错误处理

---

## 软约束

- 建议使用函数式编程风格
- 建议单个函数不超过 50 行
- 建议使用有意义的变量名

---

## 风险

- 注意并发操作可能导致的竞态条件
- 注意用户输入需要验证和清理
